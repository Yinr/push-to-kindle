version: '3'

volumes:
  uploads:

services:
  web:
    build: .
    env_file:
      - .env
    ports:
      - ${PORT}:80
    volumes:
      - uploads:/tmp/push-to-kindle/
    restart: unless-stopped
